import { getUserAuthData } from 'entities/Counter/model/selectors/getUserAuthData/GetUserAuthData';

export { userReducer, userActions } from './model/slice/userSlice';
export { UserSchema, User } from './model/types/user';
